# 개발 환경용 Dockerfile
FROM node:20-alpine

WORKDIR /app

# 패키지 파일 복사
COPY package.json package-lock.json* ./

# 의존성 설치
RUN npm ci

# 소스 코드 복사 (볼륨 마운트로 덮어씌워짐)
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
